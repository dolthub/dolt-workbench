import WorkbenchLayout from "@dsw/components/layouts/workbench";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <WorkbenchLayout params={{}}>{children}</WorkbenchLayout>;
}
